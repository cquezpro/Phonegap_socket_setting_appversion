jQuery(document).ready( function($) {
            
/*   $('.app ul li a.button').click(function(){
	  $(this).parent().find('ul.level1').slideToggle();
	  
	  $(this).toggleClass('active').parent().parent().toggleClass('singlelist');
	  
	  $('.menuburger').toggle();
	  $('body').toggleClass('text');
	  
	  if( $('.app').hasClass('open') ){
		  $('.app').animate({width:'300px'}).removeClass('open');
		  $('ul.level2.open').each(function(){ 
			$(this).animate({width: 'toggle'}).removeClass('open'); 
		  });
	  }
	  abortTimer();
	  tid = setTimeout(mycode, 3000);
	  return false;
  });
   
   $('.app ul li a.parent').click(function(){
		  if( !$('.app').hasClass('open') ){ $('.app').animate({width:'604px'}); }
		  else{ $('.app').animate({width:'300px'}); }
		  $('.app').toggleClass('open');
		  $(this).parent().find('ul.level2').animate({width: 'toggle'}).toggleClass('open');
		  return false;
   });
   
   
   $('.menuburger').click(function(){
	  $('a.button.active').trigger('click');
   });    


	// set timeout
	var tid = setTimeout(mycode, 3000);
	function mycode() {
		//alert("timeout");
		$('.menuburger').fadeOut();
		$('.main .active').removeClass('active'); 
		$('.app').find('.singlelist').removeClass('singlelist');
	    $('body').addClass('text');
		$('.app ul.level1').hide();
		//$('.main ul').hide();
		//$('.main .button').show();
		
	  // do some stuff...
	  tid = setTimeout(mycode, 3000); // repeat myself
	}
	function abortTimer() { // to be called when you want to stop the timer
	  clearTimeout(tid);
	} 
	*/
});